specVersion: "v1alpha"
serviceId: "dataconnect"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "my_db"
      cloudSql:
        instanceId: "my_instance"
connectorDirs: ["./connector"]
